---
title: "BRIC_Tutorial"
author: "Yuzhou Chang"
date: "2/3/2020"
output:
  BiocStyle::html_document:
    number_sections: no
    toc: yes
    highlight: pygments
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
# Introduction

# Download
`devtools::install_github("BMEngineeR/BRIC",force=T)`

# LTMG Track 
This is the example input from normal expression matrix which means rows stand for gene and columns stand for cell.

```{r, error=TRUE}
library(BRIC)
load("c:/analysis_work/BRIC/LTMG_track/object_LTMGTrack.RDS")
# this is example of 
# a<- read.csv("C:/Users/cha224/Documents/BRIC/inst/extdata/Pollen/Pollen_expression.csv",header = T,row.names = 1)
# object <- CreateBRICObject(as.matrix(a))
# object <- BRIC::NormalizeData(object)
# object <- AddMeta(objectm, meta.info = NULL)
```
Runing LTMG may take a while
```{r}
# LTMG suite
# object <- BRIC::RunLTMG(object,Gene_use = 2000)
print(object@LTMG@LTMG_discrete[1:5,1:5])
```

```{r}
# object <- BRIC::RunDimensionReduction(object,reduction = "tsne")
# object <- BRIC::RunClassification(object = object)
# object <- BRIC::RunClassification(object = object,resolution = 0.2)
object@MetaInfo[1:5,]
```
We can visualize the DimensionReduction result.
![dimension](c:/analysis_work/BRIC/LTMG_track/PlotDimentsion.PNG)
results:
![dimension](c:/analysis_work/BRIC/LTMG_track/cluster.PNG)
```{r}
#PlotDimension(object)
```
here is an interactive interface to select the group to compare
![findmarker](c:/analysis_work/BRIC/LTMG_track/findmarker.PNG)
```{r}
# object <- BRIC::FindMarkers(object)
# look at the marker table
object@LTMG@MarkerGene[1:5,]
```

```{r}
#PlotDimension(object)
# object <- RunPathway(object = object,customize.genelist = NULL, source = "Human", database = "GO", genes.source = "LTMG")
# look at pathway results.
object@LTMG@Pathway[1:5,]
```

